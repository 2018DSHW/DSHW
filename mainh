#ifndef MAINWINDOW_H
#define MAINWINDOW_H
#include"rtree.h"
#include <QMainWindow>

namespace Ui {
class MainWindow;
}

class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    explicit MainWindow(QWidget *parent = 0);
    ~MainWindow();
    double result;
    int index;
    int present_PID;
    int picture_num;
    int num_of_result;
    int result_of_r[200];
    int time=0;
    QString picture_ID[10000];
    double caculate(int);
    double yesorno(QString,QString);

    void loadimage();

    void findsimilar(int i);
     void test();


private:
    Ui::MainWindow *ui;
    RTree my_tree;
    QVector<int> feature[5700];
};

#endif // MAINWINDOW_H
